"""Feature engineering helpers.

Phase 1 placeholder. Real feature computations (invoice amount, tax breakdown, tax rate, unit price, frequency, etc.) will be implemented in Phase 4.
"""

import pandas as pd


def placeholder_features(df: pd.DataFrame) -> pd.DataFrame:
    """Return a shallow copy for now.

    Replace with concrete feature creation in Phase 4.
    """
    return df.copy()
